<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//PT-BR"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="br.com.usjt.partiu.service.UsuarioMapper">

    <resultMap id="result" type="string"></resultMap>
    
	<select id="findMatriculaDemitidosAssaiUltimos10Dias" resultType="list" resultMap="result"> 
		SELECT MATRICULA_ATUAL FROM conecta.TRANSACIONAL_SAU where DTA_DESLIGAMENTO is not null and trunc(DTA_DESLIGAMENTO) > trunc(sysdate - 10) and BANDEIRA like '%ASSAI%'
	</select>
</mapper>